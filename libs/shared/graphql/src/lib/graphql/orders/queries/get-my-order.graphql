query GetMyOrder($id: ID!) {
    myOrder(id: $id) {
      id
      status
      totalAmount
      createdAt
      updatedAt
      items {
        id
        productId
        quantity
        unitPrice
      }
    }
  }
